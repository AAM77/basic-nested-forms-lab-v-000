<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %><br>
  <br>
  
  <%= f.fields_for :recipes do |recipe| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>
  <% end %>
  
  <%= f.submit %>
  
<% end %>
